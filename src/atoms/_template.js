import { LitElement, html, css } from '../../static/lit-core.min.js';

export class Template extends LitElement {
  static styles = css``;

  render() {
    return html``;
  }
}

customElements.define('srcl-template', Template);
